__pycache__/
*.pyc
.venv/

# videos (large: user recordings + reference downloads)
*.mp4
!design/opendesign-landing/hero-demo.mp4

# throwaway analysis scripts (logic to be promoted into kovaak_tracker/)
_tmp_*.py

# macOS filesystem metadata
.DS_Store

# webapp local SQLite (dev/test 产物)
aiming_cookie*.db
aiming_cookie*.db-shm
aiming_cookie*.db-wal

# taste-skill (frontend design skill — development tool, not project code)
.agents/
.claude/skills/
skills-lock.json

# superpowers brainstorm mockups (design exploration, not project code)
.superpowers/

# local research evidence and generated snapshots (kept outside product commits)
.firecrawl/
artifacts/

# secrets (LLM API keys etc.)
.env
.env.*
*.secret

# vendored pi (third_party/pi) — never commit installed deps
third_party/pi/node_modules/
third_party/pi/**/node_modules/
webapp/coach-runtime/node_modules/

# session recordings under data/ stay tracked (override *.mp4
!design/opendesign-landing/hero-demo.mp4 above)
!data/*.mp4
!design/opendesign-landing/hero-demo.mp4

# dev server logs
webapp/frontend/mock-*.log
webapp/frontend/*.err.log
webapp/frontend/*.out.log

# Playwright 输出目录
test-results/

# local desktop runtime data (runs, sessions, conversations, teaching state)
app-data/

# local research material (YouTube reference downloads)
viscose-youtube/

# wrangler local state
.wrangler/

# 本地参考料/运行产物（09-06 清扫出库，文件保留在本地不入库）
output/
.firecrawl/
macos-vibrancy-style-pack/
aiming-theory-compendium*.html

# 本地 agent 工作区与运行日志（09-10 审计补口：PROGRESS 09-06 声称已忽略，实际漏加）
.zcode/
*.log

# 本地参考快照与废弃素材（不入库：x76 原站 robots ai-train=no；logo.jfif 为废弃草稿）
x76-wiki-markdown-*/
*.jfif

# 构建期注入值（开源仓库不含真实中转站地址，见 provider-models.ts / build-windows-runtime.ps1）
webapp/coach-runtime/relay-base-url.local.txt
