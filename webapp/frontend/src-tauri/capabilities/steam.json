{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "external-deep-links",
  "description": "Allows the main window to open steam:// deep links (community benchmark training list) through the OS default handler. The default opener scope only covers http(s)/mailto/tel.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "steam:*" }]
    }
  ]
}
