---
name: plan-builder
description: 用户要训练计划、想系统提升、问"我该练什么"，或对现有计划想换内容时加载。规定基准诊断→短板隔离→generate_draft→activate→执行/复测闭环的编排流程与三类计划模板。
---

训练计划编排（plan builder）

计划不是任务清单，是"诊断出来的短板 + 带剂量和验收的最小处方"。流程固定：先诊断，再编排，落成草稿，激活，执行，复测。

边界：单次带课、今日练什么走 `teaching` skill；跨多天的计划编排（草稿→激活→执行→复测闭环）才走本流程。

1 先诊断（没有依据不开计划）

- 读最近的分析 overview.json 和训练历史（training_plan.review / history.trend），看当前短板和已有计划的执行情况。
- 没有任何基准数据时，第一步是让用户先跑一次基准（或拿最近几局数据当基线）——"先知道该练什么，练了才有意义"。久打不进步但一直在混战（死斗类）里泡着的，先停止加局数。
- 排查顺序不跳层：身体疼痛（走 ergonomics-check / recovery-descale 红线）→ 环境工效 → 操作习惯 → 灵敏度 → 技术缺口 → 计划。

2 三类计划模板（按诊断结果选，内容已去品牌化）

- 变异性轮换（破平台期）：把瞄准拆成若干子类（静态点击、动态点击、精准跟踪、反应跟踪、速度切换、闪避切换），每天一个子类主题，相近子类相邻排；铁律是同一子类上周玩过的场景本周必须换。适合"啥都练了就是不动"的熟手。
- 短板隔离（一次一个机能）：基准读出最弱的 1~3 项后，一次只隔离一个机能练，带反馈回路，练完回基准验证。适合诊断清楚、有明确弱项的用户。注意社区对"先补最弱还是先推最强"没有共识，两种都摆给用户选。
- 平滑度法（扫描暴露弱区间）：同一场景的多难度版本 × 从低到高多个灵敏度档各打一遍，用成绩表定位自己的弱区间，再在预测最佳区间补枪；日常开头用最弱子类热身 5~10 分钟。适合想系统性摸底的用户。首次扫描后半程分数含熟悉度红利，跨档比较要打折。

3 处方字段（每个 item 都要齐）

先查处方库，再编计划。生成计划或推荐训练前，先按用户的弱点（诊断 issue 的 signal / tags）`read knowledge/prescriptions.json`（处方小索引，整文件一次读完），用 entries 里的 topics（问题标签）、signals / metric_refs（诊断信号词）匹配；命中再 `read knowledge/entries/{entry_file}` 读全文，用条目里的推荐和场景。**处方库是首选依据，有依据就按处方推；推荐回复里不要提视频标题或出处**（"出处：《XX》"这类句子不要出现，来源字段仅供内部溯源），只给问题→练法→场景。

- 处方库没有覆盖的弱点类型：不要只回"知识库还没有这类场景的推荐"，也不要临场编场景——改用 `scenario.search`（`run_product_command`，传 query 关键词、可选 limit）搜官方场景库，结合你的瞄准知识从返回结果里挑合适的具名场景，并说明为什么适合这个弱点。**具名场景必须有真实出处：处方库、scenario.search 的官方结果、或 scenario.list 的本机清单三选一，禁止凭印象造场景名**；也可以提议联网查官方生态（`web_search`）补充说明，命中官方页面再采信。
- 场景可用性按条目里的三态如实呈现：本机可开（match=local）可以说能直接练、用户确认后帮用户打开；官方库（match=official，带 leaderboard_id）说明需先在 KovaaK 里订阅/下载；未解析（unresolved）如实说"转写存疑，未对应到官方场景"，不要当成可练场景，也不给打开承诺。
- 推荐场景时先 `scenario.list` 拿本机已装清单，优先推荐清单里的；本机清单没有的用 `scenario.search` 查官方场景库补齐，并标注"需要先在 KovaaK 里订阅/下载"。需要打开场景时**必须先问用户**（"我可以帮你打开 X，要开始练吗？"），用户确认后才调 `scenario.open`——绝不能自动打开（会弹窗打断）。本机没有的场景不要许诺打开。

- 做什么：具体到场景或做法，用户拿着就能练。
- 场景优先推荐用户本机 KovaaK 已装好的：用户提到的、以及训练卡片上不带"本机未装"标记的，都算已装，优先排这些。
- 推荐本机没装的场景时，必须如实标注"需要先在 KovaaK 里订阅/下载该场景"，不要假装开箱可练；宁可换成已装场景，也不让用户按图索骥却找不到。
- 什么时候做：挂到已有习惯上（热身时、睡前、主项之前）。
- 剂量：一次多久、练到什么程度；永远给"今天只有 5 分钟就只做这个"的兜底项。
- 验收标准：分数、动作标准或体感标准，三选一写清楚。
- 负反馈判据：如果出现某现象（要复位、越打越抖），说明什么、下一步怎么办。
- 禁止事项与复测周期：练多久回来看（一周/两周），复测看什么。

4 命令链与纪律

- 状态机是 draft → saved → active：`training_plan.generate_draft` 只产出草稿；用户确认后必须先 `training_plan.save`，成功后再 `training_plan.activate`。直接 activate 草稿会报错（cannot activate a draft plan）。改已激活计划用 training_plan.adjust / training_plan.item.add。
- plan_payload 的 items 用口语化字段（name / what / when / dose / acceptance / negative_feedback / forbidden / retest），每份计划 1-3 条；系统会把它们投影成顶栏训练卡。
- 一次只激活一个主要方向（一个模板或一个主 cue），执行和复测用 training_plan.execution.record / training_plan.retest.record 回填；复测只回答"这个方向这一轮有没有帮助"，不等于学会。
- 交付措辞用"新增而非推翻"：不说你原来的练法错了，说"新增一个习惯/一块内容，融进你原有的操作里"。
- 过了周期还没到验收标准，先和用户复述当前感受再决定：换内容、降难度或回到诊断，不自动加量。
