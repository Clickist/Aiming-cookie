# 落地页真值化口径更新清单

> **建立 2026-08-31 · 状态：待执行（等 hero 视频去留拍板 + 新版本号）**
> 依据：数据管线迁移——CV 视频图像识别 → 引擎级真值遥测（目标位置/视角/尺寸/出生死亡为游戏内部真值；击杀点击角差实测中位 0.2-0.5°；全场景覆盖）。
> 行号均指 `design/opendesign-landing/index.html`；`aiming-cookie-landing.html` 为字节级镜像，**所有改动双文件同步**（PROGRESS 约定）。

## 一、发版当天必须动（不改即失实/断链）

| # | 位置 | 现状 | 改法 |
|---|---|---|---|
| 1 | L638（两文件各一处） | 下载链接指向 `Aiming.Cookie_0.1.11_x64-setup.exe` | 换新版本号 + R2 上传；这是全站唯一版本号维护点 |
| 2 | **L757 hero Coach 对话整段** | 旧版 CV 自我坦白：「6 个固定小球在视频里都能稳定检测出来……但回答不了"准星离靶心偏了多少"——靶心相对误差这一层证据不可用」 | 全页最尖锐的过时句。重写真值口径：原"不可用"翻转为卖点（准星到目标真实角差、中位 0.2-0.5°、逐发归因） |
| 3 | L779 caption | 「讲解来自真实 analysis:10 对话」锚定旧 CV 分析产物 | 与 L747-765 对话素材联动更换；改用新真实分析或去掉编号锚定 |
| 4 | L7 / L21 / L606（meta description / og:description / JSON-LD） | 数据源三件套封闭式枚举「Raw Input + 录屏 + KovaaK Stats/Performance」 | 更新为含引擎真值的口径；发版当天改可触发 SEO 重抓，避免搜索引擎缓存旧话术 |
| 5 | L636 hero lead、L827 采集清单 | 同样枚举三件套 | 同上口径统一 |

另：L800-802 对照表「数据来源」行、L856-952 采集 mock 的 `✓/−视频/Raw` 徽章与「多源模式」（L886）——旧"多源可用性"语义（缺源降级三档）随数据源迁移改变，发版版至少核对不失实，重设计可后置。

## 二、hero-demo.mp4 去留（待点点拍板）

- 视频为 60s/1080p60，录的是**旧 CV 版分析 run 54020（1wall 6targets small）的真实过程**；页面 L751/758/765 的对话卡片带 `data-ms` 时间轴 seeklink 指向视频内时刻——**视频 + 对话 + seeklink 是一个原子单元，不能只改一头**。
- 方案 A（推荐）：用真值化稳定版**重录**（brief L160 本就约定"正式发布前用稳定版重录"）；L757/779 文案 + 全部 seeklink 同步重做。
- 方案 B（来不及时）：保视频，但 L747-765 对话整体重写为不依赖画面检测的口径，caption 去 analysis 编号锚定；并确认视频内无「视频分析中：逐帧识别靶子」类过程画面特写。
- 视频时长数据（`data-duration="60"`、seek max）与现视频吻合，重录保持 60s 可少动播放器参数。

## 三、可以慢慢做的话术升级（不失实，欠强）

1. 品牌主线：「一手原始数据」→「**引擎级真值遥测**」（"一手"仍真，降为次要属性）。落点：L636、L787-788 对照块标题/lead、L1063 Coach 支柱。
2. 对照表（L800-814）：「数据来源」行加入引擎真值；「结论粒度」从「精确到每一次减速、每一段轨迹」升到「**精确到每一发 + 真实反应时 + 屏幕外目标**」；可选加一行"估计值 vs 真值"维度。
3. L831 覆盖行：从列举四类家族升为「**全场景覆盖，无检测失败**」——CV 短板的负转正点。
4. L1026-1044 指标表：新增目标侧真值指标行（角差/真实反应时/逐发归因），L1047 理论来源行对应扩充。
5. L663-696 会话栏 mock 摘要：换真值化后的真实样例（可含逐发/角差类信息）。
6. **`docs/landing-brief.md` 回写**（L18/L36-37"数据精度"优势线定义）——它是对外文案的内容合同，不改会继续产出旧口径。
7. `aiming-cookie-landing-alt.html`（08-19 旧备选）归档或标注废弃，避免误用。

## 四、明确不动

H1（L635）、「分数会骗人」整块（L966-1008）、现有四个运动学指标行、Coach 私教/外设支柱、「Coach 需自备 LLM provider」、「**数据只留在你的电脑上**」（遥测同为本地读本地存，承诺成立；发版前重确认一次表述）、页脚、CSS/JS、robots/sitemap/favicon/brand-spec。

## 五、维护注意

- 双文件字节一致是硬约定（改完 `cmp` 验证）。
- 版本号只在这两处 L638，无脚本自动同步；无 changelog 块——本大版本若要对外展示"数据源升级"，需在 hero CTA 附近临时加一行更新说明（页面现有结构无承载位）。
- 执行顺序建议：与发版 runbook 合并走（实机测试 → 提交 → 本清单 + 视频决策 → 打包 → R2 → 落地页双文件 → CF Pages）。
